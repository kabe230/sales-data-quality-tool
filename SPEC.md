# 製品仕様

## 1. 文書の位置づけ

この文書は、営業案件データ品質チェックツールの現行仕様を、実装と自動テストに
合わせて整理したものです。仕様変更時は、この文書と対応するテストを同じ変更で
更新します。

- 現行仕様: 本文書、ソースコード、自動テスト
- 固定受入例: [SAMPLE_DATA_SPEC.md](SAMPLE_DATA_SPEC.md)
- 完了条件: [ACCEPTANCE_CRITERIA.md](ACCEPTANCE_CRITERIA.md)
- 当初の要求: [仕様書.md](仕様書.md)

## 2. 目的と対象範囲

CSVまたはExcelで受け取った営業案件データを、ブラウザ上で整形、検証、集計し、
結果をCSVおよびExcelとして出力します。

対象範囲:

- ファイルの事前検査と読込
- 一意に決定できる値だけの自動整形
- 業務ルールによる指摘と行単位の判定
- ERROR行を除外した集計
- 修正履歴、指摘根拠、処理情報を含むレポート出力

対象外:

- アップロードデータの永続保存
- データベース連携、ユーザー認証、権限管理
- 原本ファイルの上書き
- 推測を伴うデータ補完

## 3. 入力

### 3.1 ファイル要件

| 項目 | 仕様 |
|---|---|
| 対応形式 | `.csv`、`.xlsx` |
| CSV文字コード | UTF-8（BOM有無を含む）、Shift-JIS系を自動判定 |
| 最大ファイルサイズ | 10 MiB |
| 最大データ行数 | 10,000行 |
| ヘッダー | 必須11列。列順は任意 |
| 仕様外の列 | 入力順を保って整形済みデータへ引き継ぐ |
| 予約列 | 内部結果列と同名の列は入力不可 |

空ファイル、読込不能なファイル、必須列不足、予約列の使用、行数超過は処理開始前に
エラーとして扱います。

### 3.2 入力列

| 日本語列名 | 内部名 | 必須 | ルール |
|---|---|---:|---|
| 案件ID | `project_id` | はい | 英大文字で開始し、英大文字・数字・`_`・`-`で50文字以内 |
| 顧客名 | `customer_name` | はい | 空でない文字列 |
| 担当者名 | `contact_person` | はい | 空でない文字列 |
| メールアドレス | `email` | はい | 空白なし、`@`が1個、ドメインに`.`を含む簡易業務形式 |
| 案件名 | `project_name` | はい | 空でない文字列 |
| 案件金額 | `amount` | はい | 0以上999,999,999,999,999以下の整数 |
| ステータス | `status` | はい | 許可値のいずれか |
| 担当部署 | `department` | いいえ | 未入力時はWARNING |
| 登録日 | `registered_date` | はい | 対応日付形式で、実行基準日以前 |
| 受注予定日 | `expected_order_date` | 条件付き | 商談中・見積提出では必須 |
| 備考 | `notes` | いいえ | 任意文字列 |

処理結果には、`source_row_number`、`check_result`、`error_count`、
`warning_count`、`issue_summary`を付加します。

## 4. 自動整形

処理順は、空値判定、改行統一、前後空白除去、構造化項目の全角数字変換、
項目固有の正規化です。変化したセルごとに元の値、修正後の値、修正コードを
記録します。

### 4.1 項目別の処理

- 案件ID: 全角英字・数字・記号を半角化し、ASCII英字を大文字化
- メールアドレス: 全角英字・`＠`・`．`を半角化し、ASCII英字を小文字化
- 案件金額: 全角数字、`￥`、`¥`、3桁カンマ、末尾の`円`を安全に除去して整数化
- 日付: `YYYY-MM-DD`、`YYYY/MM/DD`、`YYYY年M月D日`を日付へ変換
- ステータス: 次の別名だけを許可値へ変換

| 入力 | 変換後 |
|---|---|
| 見積り提出、見積提出済 | 見積提出 |
| 受注済、成約 | 受注 |
| 失注済 | 失注 |
| 一時保留 | 保留 |

日付の汎用推測や、誤記と考えられる値の推測修正は行いません。

### 4.2 修正コード

`NORMALIZED_PROJECT_ID`、`NORMALIZED_EMAIL`、`NORMALIZED_AMOUNT`、
`NORMALIZED_DATE`、`NORMALIZED_STATUS`、`NORMALIZED_LINE_BREAK`、
`NORMALIZED_FULL_WIDTH_NUMBER`、`TRIMMED_TEXT`

## 5. 検証と行判定

許可ステータスは、`新規`、`商談中`、`見積提出`、`受注`、`失注`、`保留`です。
重複案件IDは、重複している全行をERRORとします。

### 5.1 ERROR

| コード | 条件 |
|---|---|
| `EMPTY_REQUIRED_VALUE` | 必須値が空 |
| `INVALID_PROJECT_ID` | 案件IDが規定形式外 |
| `DUPLICATE_PROJECT_ID` | 案件IDが重複 |
| `INVALID_EMAIL` | メール形式が不正 |
| `INVALID_AMOUNT` | 整数として解釈不能 |
| `NEGATIVE_AMOUNT` | 金額が負数 |
| `AMOUNT_OUT_OF_RANGE` | 金額が上限超過 |
| `INVALID_DATE` | 対応形式の日付として解釈不能 |
| `FUTURE_REGISTERED_DATE` | 登録日が実行基準日より未来 |
| `UNKNOWN_STATUS` | ステータスが許可値外 |
| `EXPECTED_DATE_MISSING` | 商談中・見積提出なのに受注予定日が空 |

### 5.2 WARNING

| コード | 条件 |
|---|---|
| `PAST_EXPECTED_ORDER_DATE` | 受注予定日が実行基準日より過去 |
| `EXPECTED_DATE_BEFORE_REGISTERED_DATE` | 受注予定日が登録日より前 |
| `MISSING_DEPARTMENT` | 担当部署が空 |

行判定は、ERRORが1件以上なら`ERROR`、ERRORなしでWARNINGが1件以上なら
`WARNING`、どちらもなければ`NORMAL`です。1行に複数の指摘を保持できます。

## 6. 集計

`NORMAL`と`WARNING`を集計対象とし、`ERROR`はすべての集計から除外します。

出力する集計:

- ステータス別: 件数、合計金額、平均金額。許可値6種を0件も含めて表示
- 顧客別: 件数、合計、平均、最大、最小
- 担当部署別: 件数、合計、平均。空欄は`未設定`
- 登録月別: 件数、合計、平均
- 受注予定月別: 見込件数、見込金額
- 指摘内訳: 重要度・コード別の指摘件数と該当行数

受注予定月別の対象ステータスは、`新規`、`商談中`、`見積提出`、`保留`です。
平均金額は小数第2位までのDecimal値として扱います。

## 7. 出力

### 7.1 CSV

- UTF-8 BOM付き
- 日本語ヘッダー
- CRLF改行
- 行インデックスなし
- 元の11列、仕様外の列、内部結果列の順

### 7.2 Excel

シート順は次の9枚で固定します。

1. 概要
2. 整形済みデータ
3. 指摘一覧
4. 修正履歴
5. ステータス別集計
6. 顧客別集計
7. 担当部署別集計
8. 登録月別集計
9. 受注予定月別集計

概要には処理基本情報、基本指標、指摘内訳を含めます。各シートにはフィルター、
見出し固定、列幅調整を適用し、整形済みデータのERROR行とWARNING行を色分けします。

### 7.3 出力時の安全対策

文字列の先頭（半角・全角空白、タブ、改行を除いた先頭）が`=`、`+`、`-`、`@`
の場合、CSV／Excelの出力値にアポストロフィを付けます。内部データは変更せず、
表計算ソフトによる数式実行だけを防止します。

## 8. 構成と品質要件

Streamlitの画面処理と業務ロジックを分離し、読込、整形、検証、集計、出力を
独立モジュールとしてテスト可能にします。

品質ゲート:

- Ruffのlintとformat checkが成功
- 全自動テストが成功
- `src/sales_data_quality`のテストカバレッジ80%以上
- 10,000行をカバレッジ計測下で15秒以内に処理
- 固定12件の受入結果が期待値と完全一致

