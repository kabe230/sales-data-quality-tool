# 開発指示書：売上・案件データ品質チェックツール

## 1. 開発目的

CSVまたはExcelで管理されている売上・案件データを読み込み、以下を自動化するWebツールを開発してください。

- データ形式の統一
- 入力ミスの検出
- エラー・警告の分類
- 自動修正履歴の記録
- 正常データを使った集計
- Excel・CSVレポートの出力

本ツールはポートフォリオとして公開するため、単に動作するだけではなく、以下を重視してください。

- 読みやすい設計
- 責務の分離
- 型ヒント
- エラーハンドリング
- テストコード
- 分かりやすいREADME
- 実務で使える画面と出力ファイル

不明点がある場合は、勝手に仕様を変更せず、実装前に質問してください。

---

## 2. 使用技術

以下を基本構成としてください。

- Python 3.12
- Streamlit
- pandas
- openpyxl
- PyYAML
- charset-normalizer
- pytest
- pytest-cov
- ruff

依存関係は `pyproject.toml` で管理してください。

---

## 3. プロジェクト構成

以下の構成で作成してください。

```text
sales-data-quality-tool/
├─ app.py
├─ pyproject.toml
├─ README.md
├─ .gitignore
│
├─ src/
│  └─ sales_data_quality/
│     ├─ __init__.py
│     ├─ constants.py
│     ├─ exceptions.py
│     ├─ models.py
│     ├─ config.py
│     │
│     ├─ loaders/
│     │  ├─ __init__.py
│     │  ├─ csv_loader.py
│     │  └─ excel_loader.py
│     │
│     ├─ processing/
│     │  ├─ __init__.py
│     │  ├─ column_normalizer.py
│     │  ├─ cleaner.py
│     │  ├─ validator.py
│     │  └─ aggregator.py
│     │
│     ├─ reporting/
│     │  ├─ __init__.py
│     │  ├─ excel_exporter.py
│     │  └─ csv_exporter.py
│     │
│     └─ services/
│        ├─ __init__.py
│        └─ data_quality_service.py
│
├─ config/
│  └─ validation_rules.yaml
│
├─ tests/
│  ├─ conftest.py
│  ├─ test_column_normalizer.py
│  ├─ test_cleaner.py
│  ├─ test_validator.py
│  ├─ test_aggregator.py
│  ├─ test_exporter.py
│  └─ test_data_quality_service.py
│
├─ sample_data/
│  ├─ valid_projects.csv
│  ├─ invalid_projects.csv
│  └─ projects_sample.xlsx
│
├─ assets/
│  └─ screenshots/
│
└─ output/
   └─ .gitkeep
```

---

## 4. 設計ルール

依存方向は以下にしてください。

```text
app.py
  ↓
DataQualityService
  ↓
column_normalizer / cleaner / validator / aggregator
  ↓
models / config / constants
```

レポート生成は以下の流れにしてください。

```text
app.py
  ↓
excel_exporter / csv_exporter
  ↓
ProcessingResult
```

以下は禁止します。

- `app.py` に検証ロジックを直接書く
- `validator.py` からStreamlitを呼ぶ
- `processing` からExcel出力処理を呼ぶ
- 元のDataFrameを直接変更する
- 各所へ同じ固定値やエラーコードを重複記述する
- 例外を握りつぶす
- 利用者画面へスタックトレースを表示する

画面、業務ロジック、ファイル入出力の責務を分離してください。

---

## 5. 入力ファイル仕様

対応ファイルは以下です。

- `.csv`
- `.xlsx`

最大ファイルサイズは10MBとしてください。

CSVについては、以下の文字コードに対応してください。

- UTF-8
- UTF-8 BOM
- Shift-JIS
- 自動判定

Excelの場合は、利用者が対象シートを選択できるようにしてください。

---

## 6. 入力データの列

| 日本語列名 | 内部名 | 必須 |
|---|---|---:|
| 案件ID | project_id | 必須 |
| 顧客名 | customer_name | 必須 |
| 担当者名 | representative_name | 必須 |
| メールアドレス | email | 必須 |
| 案件名 | project_name | 必須 |
| 案件金額 | amount | 必須 |
| ステータス | status | 必須 |
| 受注予定日 | expected_order_date | 任意 |
| 登録日 | registered_date | 必須 |
| 担当部署 | department | 任意 |
| 備考 | notes | 任意 |

任意列が存在しない場合は、空列として自動追加してください。

列名の正規化後、内部処理では英語の内部名を使用してください。

---

## 7. 列名の正規化

列名の前後にある半角・全角空白を削除してください。

以下の列名は正式名称へ変換してください。

```yaml
案件 ID: 案件ID
顧客: 顧客名
会社名: 顧客名
担当者: 担当者名
メール: メールアドレス
金額: 案件金額
進捗: ステータス
登録年月日: 登録日
受注見込日: 受注予定日
```

複数の入力列が、正規化後に同じ正式列になる場合は、自動統合しないでください。

例：

```text
顧客
会社名
```

この2列が同時に存在し、両方が `顧客名` へ変換される場合は、`DUPLICATE_COLUMN` として処理を中止してください。

---

## 8. 許可ステータス

以下を許可値としてください。

```text
新規
商談中
見積提出
受注
失注
保留
```

以下の表記は自動補正してください。

```yaml
商談: 商談中
見積: 見積提出
見積り提出: 見積提出
受注済: 受注
受注済み: 受注
失注済: 失注
ペンディング: 保留
```

上記以外の値を類推して自動修正しないでください。

---

## 9. 自動整形仕様

安全かつ一意に修正できるものだけを自動整形してください。

### 9-1. 共通文字列

- 前後の半角・全角空白を削除する
- 空白のみの文字列は未入力として扱う
- `NaN` は未入力として扱う
- 本文中のスペースは削除しない
- 改行コードを統一する

例：

```text
"  株式会社青葉  " → "株式会社青葉"
```

### 9-2. 案件ID

- 前後空白を削除する
- 英字を大文字化する

例：

```text
" p-001 " → "P-001"
```

案件IDの書式自体は固定しないでください。

### 9-3. メールアドレス

- 前後空白を削除する
- 全角英数字や記号を半角化する
- 小文字へ統一する

例：

```text
" SALES@EXAMPLE.COM " → "sales@example.com"
```

メールアドレスの誤字やドメインを推測して修正しないでください。

### 9-4. 案件金額

以下の文字を削除し、数値へ変換してください。

- `¥`
- `￥`
- `円`
- カンマ
- 前後空白

全角数字を半角数字へ変換してください。

例：

```text
"￥300,000円" → 300000
"１０００００" → 100000
" ￥３００,０００円 " → 300000
```

小数が入力された場合は保持し、勝手に四捨五入しないでください。

数値化できない場合はエラーとしてください。

### 9-5. 日付

以下の形式を読み込んでください。

```text
2026/7/1
2026-07-01
2026年7月1日
2026.07.01
```

内部では日付型として保持してください。

画面とExcel出力は以下へ統一してください。

```text
yyyy/mm/dd
```

以下のような曖昧な形式は、自動推測しないでください。

```text
07/01/26
1/7
```

曖昧な日付は `INVALID_DATE` としてください。

### 9-6. ステータス

前述の表記揺れ一覧に含まれる値だけを自動補正してください。

---

## 10. 自動修正履歴

自動整形を行った場合は、必ず修正履歴を記録してください。

修正履歴の項目は以下です。

```text
行番号
案件ID
対象項目
修正コード
修正前
修正後
修正内容
```

修正コードは以下を使用してください。

```text
TRIMMED_TEXT
NORMALIZED_PROJECT_ID
NORMALIZED_EMAIL
NORMALIZED_AMOUNT
NORMALIZED_DATE
NORMALIZED_STATUS
NORMALIZED_FULL_WIDTH_NUMBER
```

同じセルに複数の細かな補正がある場合は、利用者が理解しやすい単位で1件の修正履歴へまとめても構いません。

例：

```text
" ￥３００,０００円 " → 300000
```

この場合は、`NORMALIZED_AMOUNT` として1件にまとめて構いません。

---

## 11. ファイル全体のエラー

以下のエラーは処理を中止し、利用者向けの日本語メッセージを表示してください。

| エラーコード | 条件 |
|---|---|
| UNSUPPORTED_FILE_TYPE | CSV・XLSX以外 |
| EMPTY_FILE | データ行が0件 |
| FILE_READ_ERROR | ファイル読み込み失敗 |
| ENCODING_ERROR | CSV文字コードを判定できない |
| MISSING_REQUIRED_COLUMN | 必須列が不足 |
| DUPLICATE_COLUMN | 正規化後に列名が重複 |
| EXCEL_SHEET_NOT_FOUND | 指定シートが存在しない |

利用者画面にはPythonの例外名やスタックトレースを表示しないでください。

例：

```text
必須列「案件金額」「登録日」が見つかりません。
元ファイルの見出しを確認してください。
```

内部ログには以下を記録してください。

- 発生日時
- 例外種類
- エラー内容
- 対象ファイル名
- スタックトレース

---

## 12. 行単位の検証仕様

### 12-1. 共通

| エラーコード | 条件 | 重要度 |
|---|---|---|
| EMPTY_REQUIRED_VALUE | 必須項目が未入力 | ERROR |
| DUPLICATE_PROJECT_ID | 案件IDが重複 | ERROR |

案件IDが重複している場合は、後ろの行だけではなく、該当するすべての行へエラーを付与してください。

重複行を自動削除しないでください。

### 12-2. メールアドレス

| エラーコード | 条件 | 重要度 |
|---|---|---|
| INVALID_EMAIL | 一般的なメール形式を満たさない | ERROR |

RFC完全準拠ではなく、一般業務向けの形式チェックで構いません。

### 12-3. 案件金額

| エラーコード | 条件 | 重要度 |
|---|---|---|
| INVALID_AMOUNT | 数値へ変換できない | ERROR |
| NON_POSITIVE_AMOUNT | 0以下 | ERROR |
| UNUSUALLY_HIGH_AMOUNT | 1億円以上 | WARNING |

1億円以上の案件は処理対象から除外せず、WARNINGとして扱ってください。

### 12-4. ステータス

| エラーコード | 条件 | 重要度 |
|---|---|---|
| UNKNOWN_STATUS | 許可ステータスに存在しない | ERROR |

### 12-5. 登録日

| エラーコード | 条件 | 重要度 |
|---|---|---|
| INVALID_DATE | 日付へ変換できない | ERROR |
| FUTURE_REGISTERED_DATE | 実行日より未来 | ERROR |

### 12-6. 受注予定日

受注予定日は任意項目なので、未入力だけでは通常エラーにしないでください。

| エラーコード | 条件 | 重要度 |
|---|---|---|
| INVALID_DATE | 値があるが日付へ変換できない | ERROR |
| ORDER_DATE_BEFORE_REGISTERED_DATE | 登録日より前 | WARNING |
| EXPECTED_DATE_MISSING | 商談中・見積提出だが未入力 | WARNING |
| EXPECTED_DATE_PAST | 未受注案件の予定日が過去 | WARNING |

ステータスが `受注` または `失注` の場合、受注予定日が過去でも警告しないでください。

---

## 13. 行の分類

整形済みデータへ以下の列を追加してください。

```text
チェック結果
エラー件数
警告件数
指摘内容
```

分類条件は以下です。

| チェック結果 | 条件 |
|---|---|
| 正常 | ERROR・WARNINGがない |
| 警告あり | WARNINGのみ存在する |
| エラーあり | ERRORが1件以上存在する |

`指摘内容` には、該当行のエラー・警告メッセージを読みやすい形でまとめてください。

---

## 14. エラー一覧

エラー一覧には以下の項目を出力してください。

```text
行番号
案件ID
対象項目
エラーコード
重要度
エラー内容
元の値
整形後の値
```

エラーコードは開発・問い合わせ対応向け、エラー内容は一般利用者向けの日本語としてください。

例：

```text
案件金額は1円以上で入力してください。
許可されていないステータスです：「検討中」
メールアドレスの形式が正しくありません。
```

---

## 15. 集計仕様

集計対象は以下です。

- 正常：集計に含める
- 警告あり：集計に含める
- エラーあり：集計から除外する

以下の基本指標を作成してください。

```text
対象件数
正常件数
警告件数
エラー件数
正常率
集計対象件数
集計除外件数
集計対象金額
集計除外金額
平均案件金額
最大案件金額
最小案件金額
```

正常率は、以下で計算してください。

```text
正常件数 ÷ 対象件数
```

警告ありの行は集計対象ですが、正常件数には含めません。

集計除外金額は、エラー行のうち、正の数値として読み取れる案件金額だけを合計してください。

マイナス金額や数値化できない金額は、集計除外金額へ含めないでください。

以下の集計表を作成してください。

- ステータス別の件数・金額
- 顧客別の件数・金額
- 担当部署別の件数・金額
- 登録月別の件数・金額
- 受注予定月別の見込金額

MVPの画面では、最低限ステータス別集計を表示してください。

---

## 16. データモデル

最低限、以下のデータモデルを実装してください。

```python
from dataclasses import dataclass
from typing import Any

import pandas as pd


@dataclass(frozen=True)
class ValidationIssue:
    row_number: int
    project_id: str | None
    field_name: str
    error_code: str
    severity: str
    message: str
    original_value: Any
    cleaned_value: Any


@dataclass(frozen=True)
class ChangeRecord:
    row_number: int
    project_id: str | None
    field_name: str
    change_code: str
    original_value: Any
    cleaned_value: Any
    description: str


@dataclass
class ProcessingResult:
    original_data: pd.DataFrame
    cleaned_data: pd.DataFrame
    issues: list[ValidationIssue]
    changes: list[ChangeRecord]
    summaries: dict[str, pd.DataFrame]
    processing_log: dict[str, object]
```

必要に応じて以下も、dataclassまたはEnumとして実装してください。

```text
ValidationOptions
CleaningOptions
ValidationConfig
Severity
CheckResult
ErrorCode
ChangeCode
```

---

## 17. 例外クラス

最低限、以下の例外クラスを定義してください。

```python
class DataQualityError(Exception):
    """本ツールで扱う基底例外。"""


class FileLoadError(DataQualityError):
    """ファイルの読み込みに失敗した場合。"""


class MissingColumnError(DataQualityError):
    """必須列が不足している場合。"""


class DuplicateColumnError(DataQualityError):
    """正規化後に列名が重複した場合。"""
```

必要に応じて、ファイル形式、文字コード、Excelシートに関する例外も追加してください。

---

## 18. 設定ファイル

`config/validation_rules.yaml` に、以下の内容を設定してください。

```yaml
required_columns:
  - 案件ID
  - 顧客名
  - 担当者名
  - メールアドレス
  - 案件名
  - 案件金額
  - ステータス
  - 登録日

optional_columns:
  - 受注予定日
  - 担当部署
  - 備考

allowed_statuses:
  - 新規
  - 商談中
  - 見積提出
  - 受注
  - 失注
  - 保留

amount:
  high_amount_warning_threshold: 100000000

column_aliases:
  案件 ID: 案件ID
  顧客: 顧客名
  会社名: 顧客名
  担当者: 担当者名
  メール: メールアドレス
  金額: 案件金額
  進捗: ステータス
  登録年月日: 登録日
  受注見込日: 受注予定日

status_aliases:
  商談: 商談中
  見積: 見積提出
  見積り提出: 見積提出
  受注済: 受注
  受注済み: 受注
  失注済: 失注
  ペンディング: 保留
```

`config.py` でYAMLを読み込み、型のある設定オブジェクトへ変換してください。

---

## 19. ファイル読み込み

### 19-1. CSV

`csv_loader.py` では以下を実装してください。

- UTF-8対応
- UTF-8 BOM対応
- Shift-JIS対応
- 文字コード自動判定
- 空ファイル確認
- 読み込み失敗時の例外変換

想定関数：

```python
def load_csv(
    file: BinaryIO,
    encoding: str | None = None,
) -> pd.DataFrame:
    ...
```

### 19-2. Excel

`excel_loader.py` では以下を実装してください。

- シート名一覧取得
- 指定されたシートの読み込み
- 存在しないシートの検出
- 空シートの検出

想定関数：

```python
def get_excel_sheet_names(file: BinaryIO) -> list[str]:
    ...


def load_excel(
    file: BinaryIO,
    sheet_name: str,
) -> pd.DataFrame:
    ...
```

同じアップロードファイルを複数回読み込む場合に、ファイルポインタの位置へ注意してください。

---

## 20. 処理モジュールの責務

### 20-1. column_normalizer.py

以下だけを担当してください。

- 列名の前後空白削除
- 列名の表記揺れ変換
- 必須列の確認
- 任意列の自動追加
- 正規化後の重複列検出
- 内部英語名への変換

想定関数：

```python
def normalize_columns(
    dataframe: pd.DataFrame,
    config: ValidationConfig,
) -> pd.DataFrame:
    ...
```

### 20-2. cleaner.py

安全に自動修正できるものだけを担当してください。

- 文字列の前後空白削除
- 全角数字の半角化
- 案件IDの大文字化
- メールアドレスの小文字化
- 金額表記の数値化
- 日付形式の統一
- ステータス表記の補正
- 修正履歴の生成

元DataFrameを変更せず、コピーを返してください。

想定関数：

```python
def clean_dataframe(
    dataframe: pd.DataFrame,
    options: CleaningOptions,
    config: ValidationConfig,
) -> tuple[pd.DataFrame, list[ChangeRecord]]:
    ...
```

### 20-3. validator.py

検証だけを担当し、値を修正しないでください。

想定関数：

```python
def validate_dataframe(
    dataframe: pd.DataFrame,
    options: ValidationOptions,
    config: ValidationConfig,
    execution_date: date,
) -> list[ValidationIssue]:
    ...
```

必要に応じて、以下の個別関数へ分割してください。

```python
def validate_required_values(...) -> list[ValidationIssue]:
    ...

def validate_project_id_duplicates(...) -> list[ValidationIssue]:
    ...

def validate_email(...) -> list[ValidationIssue]:
    ...

def validate_amount(...) -> list[ValidationIssue]:
    ...

def validate_dates(...) -> list[ValidationIssue]:
    ...
```

### 20-4. aggregator.py

以下を担当してください。

- 基本集計
- ステータス別集計
- 顧客別集計
- 担当部署別集計
- 登録月別集計
- 受注予定月別集計
- 集計対象外金額

想定関数：

```python
def create_summaries(
    cleaned_data: pd.DataFrame,
    issues: list[ValidationIssue],
) -> dict[str, pd.DataFrame]:
    ...
```

ERRORが存在する行は通常の集計から除外してください。

---

## 21. サービスクラス

画面側は、個別の処理モジュールを直接呼ばず、`DataQualityService` だけを呼び出す設計にしてください。

```python
class DataQualityService:
    def process(
        self,
        dataframe: pd.DataFrame,
        validation_options: ValidationOptions,
        cleaning_options: CleaningOptions,
        source_filename: str,
    ) -> ProcessingResult:
        ...
```

処理順は以下です。

```text
列名正規化
↓
自動整形
↓
検証
↓
チェック結果列の追加
↓
集計
↓
処理ログ作成
↓
ProcessingResultを返却
```

元のDataFrameを変更していないことをテストしてください。

---

## 22. Streamlit画面

### 22-1. ヘッダー

タイトル：

```text
売上・案件データ 品質チェックツール
```

説明：

```text
CSV・Excelファイルをアップロードし、入力ミスの検出、
データ整形、集計レポートの作成を自動化します。
```

以下も表示してください。

- 対応形式：CSV、XLSX
- 最大ファイルサイズ：10MB
- サンプルデータのダウンロードボタン

### 22-2. サイドバー

以下を表示してください。

#### CSV文字コード

- 自動判定
- UTF-8
- Shift-JIS

#### 検証ルール

以下をON・OFFできるようにしてください。

- 必須項目チェック
- 案件ID重複チェック
- メール形式チェック
- 日付チェック
- 案件金額チェック

初期状態はすべてONです。

#### 自動整形ルール

以下をON・OFFできるようにしてください。

- 前後の空白を削除
- 全角数字を半角へ変換
- 金額表記を数値へ変換
- 日付形式を統一
- メールアドレスを小文字へ変換
- ステータスの表記揺れを修正

初期状態はすべてONです。

#### 許可ステータス

現在の許可値を表示してください。

MVPでは表示のみで構いません。

### 22-3. ファイルアップロード

CSVまたはXLSXをアップロードできるようにしてください。

Excelの場合は、読み込むシートを選択できるようにしてください。

ファイル読み込み後は、以下を表示してください。

- ファイル名
- ファイル形式
- ファイルサイズ
- 行数
- 列数
- 先頭20行のプレビュー

必要列が不足している場合は、チェック実行前に警告してください。

処理を続行できない場合は、実行ボタンを無効化してください。

### 22-4. 実行ボタン

ボタン名：

```text
データチェック・整形を実行
```

二重押下を防止してください。

処理中は進捗を表示してください。

例：

```text
データを読み込んでいます
データを整形しています
入力内容を確認しています
集計レポートを作成しています
```

### 22-5. 処理結果

以下の指標をカード形式で表示してください。

- 対象件数
- 正常件数
- 警告件数
- エラー件数
- 正常率
- 集計対象件数
- 集計除外件数
- 集計対象金額

結果を以下のタブに分けてください。

```text
概要
エラー一覧
修正履歴
整形済みデータ
集計結果
処理ログ
```

#### 概要タブ

- 指標
- ステータス別案件件数
- ステータス別案件金額
- エラー種類の内訳

MVPでは最低限、ステータス別集計を表示してください。

#### エラー一覧タブ

以下で絞り込めるようにしてください。

- 重要度
- 対象項目
- エラーコード

#### 修正履歴タブ

自動整形で変更された内容を一覧表示してください。

#### 整形済みデータタブ

整形後のデータと、以下の追加列を表示してください。

- チェック結果
- エラー件数
- 警告件数
- 指摘内容

正常データだけに絞り込める切替を用意してください。

#### 集計結果タブ

最低限、ステータス別集計を表示してください。

可能であれば以下も表示してください。

- 顧客別集計
- 担当部署別集計
- 登録月別集計
- 受注予定月別集計

#### 処理ログタブ

以下を表示してください。

- 処理開始日時
- 処理終了日時
- 処理時間
- 読み込み件数
- 正常件数
- 警告件数
- エラー件数
- 実行した自動整形
- 発生したシステムエラー

---

## 23. Excelレポート出力

以下の7シートを作成してください。

```text
概要
整形済みデータ
エラー一覧
修正履歴
ステータス別集計
月別集計
顧客別集計
```

ファイル名は以下です。

```text
sales_data_report_YYYYMMDD_HHMMSS.xlsx
```

Excelの書式は以下としてください。

- 見出しは濃い青背景
- 見出し文字は白
- 見出しは太字
- 1行目を固定
- オートフィルタを設定
- 日付は `yyyy/mm/dd`
- 金額は `¥#,##0`
- 長いエラーメッセージは折り返す
- 内容に応じた適切な列幅
- ERRORとWARNINGを見分けられる表示
- シート名は日本語

`概要` シートには以下を表示してください。

- 処理日時
- 元ファイル名
- 対象件数
- 正常件数
- 警告件数
- エラー件数
- 正常率
- 集計対象金額
- 集計除外金額
- 実行した自動整形

想定関数：

```python
def create_excel_report(
    result: ProcessingResult,
    source_filename: str,
) -> bytes:
    ...
```

---

## 24. CSV出力

以下を個別にダウンロードできるようにしてください。

- 整形済みデータCSV
- エラー一覧CSV

Excelで文字化けしにくいように、UTF-8 BOM付きで出力してください。

想定関数：

```python
def create_cleaned_csv(result: ProcessingResult) -> bytes:
    ...


def create_error_csv(result: ProcessingResult) -> bytes:
    ...
```

---

## 25. 基準サンプルデータ

12件の結合テスト用データを作成してください。

期待結果は以下です。

```text
対象件数：12件
正常件数：4件
警告件数：2件
エラー件数：6件
正常率：33.3%
集計対象件数：6件
集計除外件数：6件
集計対象金額：101,590,000円
集計除外金額：750,000円
```

サンプルデータの内訳は以下です。

| 行 | 内容 | 判定 | 金額 |
|---:|---|---|---:|
| 1 | 正常な新規案件 | 正常 | 300,000 |
| 2 | 8行目と案件ID重複 | エラー | 正の金額 |
| 3 | 正常な受注案件 | 正常 | 450,000 |
| 4 | メールアドレス不正 | エラー | 正の金額 |
| 5 | 顧客名未入力 | エラー | 正の金額 |
| 6 | 案件金額がマイナス | エラー | -50,000 |
| 7 | 商談を商談中へ自動修正 | 正常 | 250,000 |
| 8 | 2行目と案件ID重複 | エラー | 正の金額 |
| 9 | 見積提出だが受注予定日なし | 警告 | 320,000 |
| 10 | 登録日が未来日 | エラー | 正の金額 |
| 11 | 1億円案件 | 警告 | 100,000,000 |
| 12 | 全角金額・日付表記を自動修正 | 正常 | 270,000 |

エラー行の正の案件金額合計が750,000円になるように設定してください。

サンプルデータは、CSVとXLSXの両方を用意してください。

---

## 26. 必須テスト

### 26-1. 列名正規化

以下をテストしてください。

- 列名の別名が正式列へ変換される
- 前後空白が除去される
- 正規化後の重複列を検出する
- 任意列が自動追加される
- 必須列不足を検出する

### 26-2. 自動整形

以下をテストしてください。

- `￥300,000円` が `300000` になる
- 全角数字が半角になる
- 案件IDが大文字になる
- メールアドレスが小文字になる
- 日付表記が統一される
- ステータス表記が補正される
- 修正履歴が生成される
- 元DataFrameが変更されない

### 26-3. 検証

以下をテストしてください。

- 空欄の必須項目
- 空白だけの必須項目
- 重複IDのすべての行へエラーが付く
- 不正なメールアドレス
- 数値化できない案件金額
- 0以下の案件金額
- 1億円以上の案件金額への警告
- 不正なステータス
- 未来の登録日
- 受注予定日不足
- 過去の受注予定日
- 登録日より前の受注予定日
- 受注・失注案件の過去予定日は警告しない

### 26-4. 集計

以下をテストしてください。

- ERROR行を集計から除外する
- WARNING行を集計対象に含める
- ステータス別件数合計が集計対象件数と一致する
- ステータス別金額合計が基本集計金額と一致する
- 集計対象金額が101,590,000円になる
- 集計除外金額が750,000円になる

### 26-5. Excel・CSV出力

以下をテストしてください。

- 生成したExcelを正常に読み込める
- Excelに7シートが存在する
- シート名が仕様どおりである
- CSVがUTF-8 BOM付きである
- 金額書式が設定されている
- 日付書式が設定されている
- 1行目が固定されている
- オートフィルタが設定されている

### 26-6. 結合テスト

`DataQualityService.process()` に基準12件データを渡し、以下を確認してください。

- 対象件数
- 正常件数
- 警告件数
- エラー件数
- 修正履歴
- 集計対象件数
- 集計除外件数
- 集計対象金額
- 集計除外金額
- 元DataFrameが変更されていないこと

---

## 27. MVPの完了条件

以下がすべて実装されていることをMVP完成条件とします。

1. CSV・XLSXをアップロードできる
2. CSVのUTF-8・UTF-8 BOM・Shift-JISを読み込める
3. Excelの対象シートを選択できる
4. アップロードしたデータの先頭20行を確認できる
5. 必須列不足を検出して処理を中止できる
6. 列名の表記揺れを正規化できる
7. 文字列・金額・日付・ステータスを自動整形できる
8. エラーと警告を行単位で検出できる
9. 自動修正の履歴を確認できる
10. 正常・警告あり・エラーありへ分類できる
11. エラー行を除外して集計できる
12. ステータス別集計を表示できる
13. 整形済みデータを画面で確認できる
14. Excelレポートをダウンロードできる
15. 整形済みCSVをダウンロードできる
16. エラー一覧CSVをダウンロードできる
17. サンプルデータが付属している
18. テストがすべて成功する
19. READMEに利用方法と設計意図が記載されている

---

## 28. 品質基準

以下を満たしてください。

```text
pytest：全件成功
テストカバレッジ：80%以上
ruff check：エラー0件
公開関数・クラス：型ヒントあり
主要関数・クラス：docstringあり
```

pandas周辺で型検査が過度に複雑になるため、MVPでは `mypy --strict` は必須としません。

Streamlitの表示コードは、必要に応じて一部カバレッジ対象外として構いません。

ただし、業務ロジックは十分にテストしてください。

---

## 29. 性能基準

以下を目標としてください。

| 条件 | 目標 |
|---|---:|
| ファイル上限 | 10MB |
| 対応行数 | 10,000行 |
| 10,000行の処理時間 | 5秒以内を目標 |
| Excelレポート生成 | 10秒以内を目標 |

PC性能によって変動するため、絶対保証ではなく開発環境での目標値としてください。

---

## 30. README

READMEには最低限、以下を記載してください。

1. ツール概要
2. 解決する業務課題
3. 主な機能
4. 使用技術
5. セットアップ方法
6. 起動方法
7. テスト実行方法
8. サンプルデータの使い方
9. ディレクトリ構成
10. 検証・整形仕様
11. 設計上の判断
12. 画面画像を配置する場所
13. 今後の改善案

設計上の判断では、以下を説明してください。

- 元データを変更しない理由
- 安全に一意に修正できるものだけを自動化する理由
- ERROR行を集計から除外する理由
- WARNING行を集計対象へ含める理由
- 画面と業務ロジックを分離した理由
- エラーコードと利用者向けメッセージを分けた理由
- 自動修正履歴を残す理由

---

## 31. 実装の進め方

一度にすべて実装せず、以下のフェーズ順に進めてください。

### フェーズ1

- プロジェクト構成作成
- `pyproject.toml`
- 設定モデル
- データモデル
- 例外
- 定数
- 列名正規化
- 自動整形
- 検証
- 集計
- 単体テスト

### フェーズ2

- `DataQualityService`
- 結合テスト
- 基準12件データ
- 期待値の確認

### フェーズ3

- CSV読み込み
- Excel読み込み
- Excelレポート出力
- CSV出力
- 出力テスト

### フェーズ4

- Streamlit画面
- ファイルアップロード
- プレビュー
- 実行ボタン
- 結果タブ
- ダウンロード機能
- 利用者向けエラー表示

### フェーズ5

- README
- サンプルデータ
- ruff確認
- pytest確認
- カバレッジ確認
- 不要コード削除
- 最終動作確認

各フェーズ終了時に、以下を実行してください。

```bash
ruff check .
pytest
pytest --cov=src/sales_data_quality --cov-report=term-missing
```

問題がある場合は、そのフェーズ内で修正してから次へ進んでください。

---

## 32. 実装時の進行ルール

- まず既存フォルダとファイルを確認する
- 既存ファイルがある場合は、内容を確認してから変更する
- 仕様を独自解釈して大幅に変更しない
- 不明点があれば実装前に質問する
- 各フェーズの終了時に変更内容を報告する
- テスト失敗を残したまま次のフェーズへ進まない
- エラーを回避するためだけにテストを削除しない
- 実装していない機能をREADMEへ記載しない
- サンプルデータに個人情報や実在企業の情報を使用しない
- すべて架空のデータを使用する

---

## 33. 最終報告

実装完了時は、以下を報告してください。

- 実装した機能
- 作成・変更した主要ファイル
- プロジェクト構成
- テスト件数と結果
- テストカバレッジ
- ruffの結果
- アプリの起動方法
- サンプルデータの利用方法
- 出力ファイルの内容
- 未実装または制限事項
- 次に追加するとよい機能

まずはフェーズ1から開始してください。

フェーズ1の実装前に、今回作成するファイル一覧と実装方針を簡潔に提示してください。